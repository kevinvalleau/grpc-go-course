syntax = "proto3";

package calculator;

import "sum.proto";

option go_package = "github.com/kevinvalleau/grpc-go-course/calculator/proto";

service CalculatorService {
    rpc Sum(SumRequest) returns (SumResponse);
}